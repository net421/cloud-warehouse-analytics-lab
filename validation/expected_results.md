# Expected results

A successful build produces 2,400 order-grain mart rows, unique order IDs, all service ratios within valid ranges, zero source-to-fact revenue variance, non-empty carrier and forecast marts, and only `PASS` rows in `data_quality_report.csv`.
