# Nested data notes

`ARRAY<STRUCT>` can keep order headers and items together. Use `UNNEST` for item-level analysis, while retaining governed flat marts for dashboard consumption and simpler semantic models.
