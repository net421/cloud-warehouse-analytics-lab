# Snowflake cost and performance notes

Use auto-suspend, query tags, compute isolation, selective materialization, date pruning, and measured clustering decisions. Do not claim performance improvements without comparing query profiles, bytes scanned, runtime, and credits.
